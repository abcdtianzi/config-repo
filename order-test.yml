
spring:
  application:
    name: ORDER
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://192.168.230.129:3306/SpringCloud_sell?characterEncoding=utf-8&useSSL=false
    username: mmall
    password: mmall
  jpa:
    show-sql: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
      hostname: 127.0.0.1  #设置跳转host名
server:
  port: 8081
env: test

